import 'package:flutter/material.dart';

class RecentDesignsScreen extends StatelessWidget {
  const RecentDesignsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Recent Designs')),
      body: const Center(child: Text('Recent designs feature coming soon')),
    );
  }
}
